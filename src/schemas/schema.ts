// // schemas/schema.js
// import createSchema from 'part:@sanity/base/schema-creator'; // Sanity schema creator
// import schemaTypes from 'all:part:@sanity/base/schema-type'; // All available schema types
// import post from './post'; // Your custom post schema

// export default createSchema({
//   name: 'default',
//   types: schemaTypes.concat([post]), // Combine default schema types with your custom ones
// });
